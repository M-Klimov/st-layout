<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Registration
        link(href="./css/styles.min.css", rel="stylesheet")
    body
        header
            .header-mini
                span.back
                span Регистрация
                span.menu

        .content.registration
            .tabs
                span.tab.active Пользователь
                span.tab Компания

            .tab-content
                .row
                    input(type="text", placeholder="Логин").input
                    input(type="password", placeholder="Почта").input

                input(type="phone", placeholder="Телефон").input
                input(type="password", placeholder="Пароль").input
                input(type="password", placeholder="Повторить пароль").input

                .row.phone-code
                    span.button.send-sms Отправить код
                    input(type="password").input

                .checkbox
                    input(type="checkbox")
                    p Подтверждая вы соглашаетесь с политикой коныеденциальности

            .tab-content
                .row
                    input(type="text", placeholder="Логин").input
                    input(type="password", placeholder="Почта").input

                input(type="phone", placeholder="Телефон").input
                input(type="password", placeholder="Пароль").input
                input(type="password", placeholder="Повторить пароль").input

                .row.phone-code
                    span.button.send-sms Отправить код
                    input(type="password").input

                .checkbox
                    input(type="checkbox")
                    p Подтверждая вы соглашаетесь с политикой коныеденциальности

            span.button Продолжить

        script(src="./js/main.min.js")
